# Mapbox public token (starts with pk.) — get one free at https://account.mapbox.com/access-tokens/
# NEVER use a secret token (sk.*) here — this value ships to the browser.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_public_token_here
